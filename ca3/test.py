l = [1]
if(l.remove(2)):
    pass

        #find region
        x_edge = abs(self.max_x_point[-1] - self.min_x_point[-1]) / 2
        y_edge = abs(self.max_y_point[-1] - self.min_y_point[-1]) / 2

        if(x_edge <= pare[X]):
            if(y_edge <= pare[Y]):
                pass
            else:
                pass
        else:
            if(y_edge <= pare[Y]):
                pass
            else:
                pass
